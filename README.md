# uni-notes

Uniform, compact, complete university notes generated by the `/tailor-notes` Claude command. One repo for every module, one folder per module, one folder per week, one chat per week.

## Layout

```
uni-notes/
├── .claude/commands/tailor-notes.md   # the command (auto available in any Claude/Cowork session on this repo)
├── templates/
│   ├── uninotes.sty                   # ALL shared styling: change here, restyle every week
│   └── week_template.tex              # skeleton each week's .tex is built from
├── scripts/render_pages.ps1           # PDF -> page PNGs (150 DPI) and figure crops (300 DPI)
└── ES3E8/                             # one folder like this per module
    ├── module.md                      # module code + full name (drives headers)
    └── Week 01/
        ├── sources/                   # upload lecture PDFs, worksheets, solutions here
        ├── extraction/                # generated: per file vision extraction (checkpoint cache)
        ├── figures/                   # generated: cropped diagrams from the original slides
        ├── pages-cache/               # generated, gitignored: rendered page images
        └── ES3E8_Week01_Notes.md/.tex/.pdf   # generated outputs
```

## Weekly routine (Cowork)

1. Open the Cowork project for the module (its folder is this repo, or the module folder inside it).
2. Drop the week's files into `Week NN/sources/` (Cowork upload or File Explorer). Upload them **in delivery order**: the pipeline processes files by creation time.
3. Start a **new chat** and run: `/tailor-notes ES3E8 Week 5` (or just `Week 5` inside the module folder).
4. Ask follow up questions in the same chat; tutor mode answers from the generated markdown without re-reading any PDFs.
5. Next week: new chat. Old chats can be deleted safely; everything permanent lives in this repo.

## One time setup already done

- Command also installed globally at `C:\Users\mryx1\.claude\commands\tailor-notes.md` so it works in any session.
- MiKTeX (pdflatex + pdftoppm) provides compilation and page rendering.

## GitHub

Remote setup (one time):

```powershell
winget install --id GitHub.cli
gh auth login          # browser login, choose HTTPS
gh repo create uni-notes --private --source "C:\Users\mryx1\repos\uni-notes" --push
```

After that the command pushes automatically at the end of every generation run. Manual sync any time: `/tailor-notes sync`.

## Compile a week by hand (if ever needed)

```powershell
Set-Location "C:\Users\mryx1\repos\uni-notes\ES3E8\Week 05"
$env:TEXINPUTS = ".;C:\Users\mryx1\repos\uni-notes\templates;"
pdflatex -interaction=nonstopmode ES3E8_Week05_Notes.tex   # run twice
```

## Formatting contract (enforced by the command)

- Header: `MODULECODE: Module Name - Week N Notes`; page numbers centred in the footer; one page contents; tight booklet margins.
- No dashes or hyphens in prose (open book copy safety); hyphenation at line breaks disabled.
- Every equation centred with a `Term: FullName (UNITS)` variable list on first use.
- Worked examples and revision questions in Q&A format with `Step X:` explanations mirroring the official solutions.
- Key Summary section at the end of every week.
